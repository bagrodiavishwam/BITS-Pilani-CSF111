#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
    //enter string with all characters unique to avoid repition
    char s[]="abcd";
    int size=(sizeof(s)/sizeof(char));
    float count = tgamma(size+1);
}